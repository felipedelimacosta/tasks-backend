pipeline {
    agent any

    stages{

        stage('Build BackEnd'){
            steps{
            withMaven {
                    sh 'mvn clean package -DskipTests=true'
}
            }
        }
    }
}