pipeline {
    agent any
    stages{

        stage('Build BackEnd'){
            steps{
                container ('maven'){
                    sh 'mvn clean package -DskipTests=true'
                }
            }
        }
    }
}