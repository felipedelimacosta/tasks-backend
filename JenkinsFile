pipeline {
    agent any
    tools{
        maven 'Maven 3.3.9'
        jdk 'jdk8'
    }
    stages{

        stage('Build BackEnd'){
            steps{

                    sh 'mvn clean package -DskipTests=true'

            }
        }
    }
}